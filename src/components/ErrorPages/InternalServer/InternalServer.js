import React from 'react';
import './InternalServer.css';

const internalServer = (props) => {
    return (
        <p className={'internalServer'}>{"500 server error, please contact administrator"}</p>
    )
}

export default internalServer;